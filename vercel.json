{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "installCommand": "yarn install",
  "buildCommand": "yarn workspace @friends/database db:generate && yarn workspace @friends/database build && yarn workspace @friends/web build",
  "outputDirectory": "apps/web/.next"
}
